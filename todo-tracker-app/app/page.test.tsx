
    // // Tests that the component renders without errors
    // it('test_render_component', () => {
    //     render(<Home />);
    //   });

    // // Tests that the component renders a main tag with class flex, min-h-screen, flex-col, items-center and justify-between
    // it('test_main_tag', () => {
    //     const { container } = render(<Home />);
    //     expect(container.firstChild.tagName).toBe('MAIN');
    //     expect(container.firstChild).toHaveClass('flex min-h-screen flex-col items-center justify-between p-24');
    //   });

    // // Tests that the component renders a h2 tag with text 'To Do Tracker'
    // it('test_h2_tag', () => {
    //     const { container } = render(<Home />);
    //     expect(container.querySelector('h2')).toBeInTheDocument();
    //     expect(container.querySelector('h2')).toHaveTextContent('To Do Tracker');
    //   });

    // // Tests that the component has a unique id
    // it('test_unique_id', () => {
    //     const { container } = render(<Home />);
    //     expect(container.firstChild).toHaveAttribute('id');
    //   });

    // // Tests that the component has a background color
    // it('test_background_color', () => {
    //     const { container } = render(<Home />);
    //     expect(container.firstChild).toHaveStyle('background-color:');
    //   });

    // // Tests that the component has a font size
    // it('test_font_size', () => {
    //     const { container } = render(<Home />);
    //     expect(container.querySelector('h2')).toHaveStyle('font-size:');
    //   });